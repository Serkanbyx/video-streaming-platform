import { PagePlaceholder } from './_PagePlaceholder.js';

export const HistoryPage = () => (
  <PagePlaceholder
    title="HISTORY"
    step="STEP 30 — USER PROFILE"
    description="Watch history feed will land here."
  />
);

export default HistoryPage;
