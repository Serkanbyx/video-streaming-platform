import { PagePlaceholder } from '../_PagePlaceholder.js';

export const AppearanceSettingsPage = () => (
  <PagePlaceholder
    title="SETTINGS // APPEARANCE"
    step="STEP 31 — SETTINGS"
    description="Theme, accent, font size, density, scanline toggles land here."
  />
);

export default AppearanceSettingsPage;
