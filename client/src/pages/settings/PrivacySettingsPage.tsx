import { PagePlaceholder } from '../_PagePlaceholder.js';

export const PrivacySettingsPage = () => (
  <PagePlaceholder
    title="SETTINGS // PRIVACY"
    step="STEP 31 — SETTINGS"
    description="Profile visibility toggles for email, history and subscriptions land here."
  />
);

export default PrivacySettingsPage;
