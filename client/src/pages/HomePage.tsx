import { PagePlaceholder } from './_PagePlaceholder.js';

export const HomePage = () => (
  <PagePlaceholder
    title="HOME"
    step="STEP 25 — DISCOVERY GRID"
    description="Asymmetric brutalist video grid with search will land here."
  />
);

export default HomePage;
