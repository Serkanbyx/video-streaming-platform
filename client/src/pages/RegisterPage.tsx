import { PagePlaceholder } from './_PagePlaceholder.js';

export const RegisterPage = () => (
  <PagePlaceholder
    title="REGISTER"
    step="STEP 24 — AUTH PAGES"
    description="Account creation form sharing the server's validation schema."
  />
);

export default RegisterPage;
