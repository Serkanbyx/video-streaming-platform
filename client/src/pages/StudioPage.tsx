import { PagePlaceholder } from './_PagePlaceholder.js';

export const StudioPage = () => (
  <PagePlaceholder
    title="STUDIO"
    step="STEP 28 — CREATOR DASHBOARD"
    description="My videos, status tabs, edit and delete will land here."
  />
);

export default StudioPage;
