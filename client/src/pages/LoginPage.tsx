import { PagePlaceholder } from './_PagePlaceholder.js';

export const LoginPage = () => (
  <PagePlaceholder
    title="LOGIN"
    step="STEP 24 — AUTH PAGES"
    description="Email + password form using the shared Zod schema."
  />
);

export default LoginPage;
