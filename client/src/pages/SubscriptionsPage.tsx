import { PagePlaceholder } from './_PagePlaceholder.js';

export const SubscriptionsPage = () => (
  <PagePlaceholder
    title="SUBSCRIPTIONS"
    step="STEP 30 — USER PROFILE"
    description="Subscription feed lives here."
  />
);

export default SubscriptionsPage;
