import { PagePlaceholder } from '../_PagePlaceholder.js';

export const AdminUsersPage = () => (
  <PagePlaceholder
    title="ADMIN // USERS"
    step="STEP 32 — ADMIN PANEL"
    description="User table with role and status moderation lands here."
  />
);

export default AdminUsersPage;
