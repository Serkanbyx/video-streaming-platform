import { PagePlaceholder } from './_PagePlaceholder.js';

export const ProfilePage = () => (
  <PagePlaceholder
    title="PROFILE"
    step="STEP 30 — USER PROFILE"
    description="Personal profile overview lives here."
  />
);

export default ProfilePage;
